config:
  existingSecret: quay-config

app:
  enabled: true

ingress:
  enabled: true
  className: nginx
  annotations:
    example.com/provider-setting: enabled
  host: quay.example.com
  paths:
    - /
    - /v2
  tls:
    enabled: true
    secretName: quay-example-tls
