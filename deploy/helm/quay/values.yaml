image:
  repository: quay.io/projectquay/quay
  tag: latest
  digest: ""
  pullPolicy: IfNotPresent

config:
  existingSecret: ""

app:
  enabled: false
  replicaCount: 1
  entrypoint: registry-nomigrate

workers:
  enabled: false
  replicaCount: 1
  entrypoint: registry-nomigrate

migration:
  enabled: false

ingress:
  enabled: false
